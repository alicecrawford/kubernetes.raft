---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/instance: selkies-apps
  name: selkies-apps
  namespace: selkies-apps
spec:
  dnsNames:
    - apps.raft.lutris.house
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns01-issuer
  secretName: selkies-apps-certificate
