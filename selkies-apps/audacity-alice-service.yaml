---
apiVersion: v1
kind: Service
metadata:
  name: audacity-alice
  namespace: selkies-apps
spec:
  type: ClusterIP
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    name: audacity-alice
