---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-nfs-library-pvc
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-ssd-csi
  resources:
    requests:
      storage: 2Ti
