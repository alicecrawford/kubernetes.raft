---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: code-server
  namespace: code-server
spec:
  dnsNames:
    - code-server.raft.lutris.house
  secretName: code-server-certificate
  issuerRef:
    name: letsencrypt-dns01-issuer
    kind: ClusterIssuer
