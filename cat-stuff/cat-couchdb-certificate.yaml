---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cat-couchdb
  namespace: cat-stuff
spec:
  dnsNames:
    - cat-couchdb.raft.lutris.house
  secretName: cat-couchdb-certificate
  issuerRef:
    name: letsencrypt-dns01-issuer
    kind: ClusterIssuer
