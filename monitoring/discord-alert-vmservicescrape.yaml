apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  labels:
    name: discord-alert
  name: discord-alert
  namespace: monitoring
spec:
  attach_metadata: {}
  endpoints:
    - attach_metadata: {}
      port: discord-alert-www
  namespaceSelector:
    matchNames:
      - monitoring
  selector:
    matchLabels:
      name: discord-alert
