apiVersion: operator.victoriametrics.com/v1beta1
kind: VMRule
metadata:
  name: rule-k8s-rules-container-memory-working-set-bytes
  namespace: monitoring
spec:
  groups:
  - name: k8s.rules.container_memory_working_set_bytes
    rules:
    - alert: ''
      expr: container_memory_working_set_bytes{job="kubelet",metrics_path="/metrics/cadvisor",image!=""}
        * on(polycule,namespace,pod) group_left(node) topk(1, max(kube_pod_info{node!=""})
        by(polycule,namespace,pod,node)) by(polycule,namespace,pod)
      record: node_namespace_pod_container:container_memory_working_set_bytes
