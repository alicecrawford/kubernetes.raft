---
apiVersion: v1
kind: Service
metadata:
  name: discord-alert
  namespace: monitoring
  labels:
    name: discord-alert
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: discord-alert-www
  selector:
    name: discord-alert
