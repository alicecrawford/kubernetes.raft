---
prometheus:
  podMonitor:
    enabled: true
    relabelings:
      - sourceLabels: [__meta_kubernetes_pod_node_name]
        targetLabel: instance
        action: replace
        replacement: $1
      - targetLabel: job
        action: replace
        replacement: node-exporter
