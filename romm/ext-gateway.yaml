---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: ext-romm-gateway
  namespace: romm
spec:
  gatewayClassName: istio
  listeners:
    - name: http
      port: 443
      protocol: HTTPS
      allowedRoutes:
        namespaces:
          from: Same
      tls:
        certificateRefs:
          - kind: Secret
            group: ""
            name: ext-romm-certificate
