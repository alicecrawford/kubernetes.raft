---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cryptpad-blob
  namespace: cryptpad
spec:
  accessModes:
    - "ReadWriteOnce"
  storageClassName: longhorn
  resources:
    requests:
      storage: "2Gi"
