---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cryptpad-data
  namespace: cryptpad
spec:
  accessModes:
    - "ReadWriteOnce"
  storageClassName: longhorn
  resources:
    requests:
      storage: "2Gi"
