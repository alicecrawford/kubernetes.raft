---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cryptpad
  namespace: cryptpad
spec:
  dnsNames:
    - cryptpad.raft.lutris.house
  secretName: cryptpad-certificate
  issuerRef:
    name: letsencrypt-dns01-issuer
    kind: ClusterIssuer
