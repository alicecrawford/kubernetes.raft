---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn
  namespace: longhorn-system
spec:
  dnsNames:
    - longhorn.raft.lutris.house
  secretName: longhorn-certificate
  issuerRef:
    name: letsencrypt-dns01-issuer
    kind: ClusterIssuer
